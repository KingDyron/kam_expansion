This folder contains LIBX files with all the unicode characters that are needed by that language.
The FontX Generator uses this to include all the characters into the unicode fonts.
All other .LIBX files that are used by the game get scanned too.